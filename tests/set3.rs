mod tests {
  use std::collections::HashMap;
  use std::fs;
  use std::str;

  use matasano::aes::{Decrypter, Encrypter, CBC, ECB};
  use matasano::cbc;
  use matasano::ecb;
  use matasano::*;

  #[test]
  fn challenge17() {
  }
}